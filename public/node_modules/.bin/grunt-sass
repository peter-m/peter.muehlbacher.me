#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-sass/bin/grunt-sass" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-sass/bin/grunt-sass" "$@"
  ret=$?
fi
exit $ret
